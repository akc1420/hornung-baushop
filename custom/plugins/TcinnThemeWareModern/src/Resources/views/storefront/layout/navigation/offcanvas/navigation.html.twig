{% sw_extends '@Storefront/storefront/layout/navigation/offcanvas/navigation.html.twig' %}

{# ---------------- START: SET TEMPLATE VARS ---------------- #}

{# ThemeWare: Set theme variables #}
{% set twtOffcanvasNavServiceMenuShow = theme_config('twt-offcanvas-nav-service-menu-show') %}

{# ---------------- END: SET TEMPLATE VARS ---------------- #}

{% block layout_navigation_offcanvas_navigation_actions %}
    {# ThemeWare: Show service menu #}
    {% if twtOffcanvasNavServiceMenuShow == 2 %}
        <nav class="nav navigation-offcanvas-actions">
            {{ block('layout_navigation_offcanvas_navigation_action_language') }}

            {{ block('layout_navigation_offcanvas_navigation_action_currency') }}

            {# TODO: Fix dropdown arrow + icon and label visibility... #}

            {# ThemeWare®: Show "Service menu" as dropdown #}
            {% block twt_layout_navigation_offcanvas_navigation_action_service %}
                {% sw_include '@Storefront/storefront/layout/header/actions/service-menu-widget.html.twig' with { position: 'offcanvas' } %}

                {# TODO: Check with Shopware v6.5 #}
                {# % sw_include '@Storefront/storefront/themeware/twt-service-menu-widget.html.twig' % #}
            {% endblock %}
        </nav>
    {% else %}

        {# Default block #}
        {{ parent() }}
    {% endif %}
{% endblock %}
